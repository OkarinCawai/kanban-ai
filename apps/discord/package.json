{
  "name": "@kanban/discord",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/src/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "cross-env DOTENV_CONFIG_PATH=../../.env node dist/src/main.js",
    "test": "npm run build && node --test --test-isolation=none \"dist/test/**/*.test.js\"",
    "lint": "echo No lint configured for @kanban/discord"
  },
  "dependencies": {
    "@kanban/utils": "0.1.0",
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "dotenv": "^16.4.7",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  }
}


