{
  "name": "@kanban/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/src/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test --test-isolation=none \"dist/test/**/*.test.js\"",
    "lint": "echo No lint configured for @kanban/contracts"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}


