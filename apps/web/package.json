{
  "name": "@kanban/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test --test-isolation=none \"dist/test/**/*.test.js\"",
    "start": "node dist/src/dev-server.js",
    "lint": "echo No lint configured for @kanban/web"
  }
}


