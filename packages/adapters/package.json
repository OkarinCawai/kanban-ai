{
  "name": "@kanban/adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/src/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build --workspace @kanban/contracts && npm run build --workspace @kanban/core && npm run build && node --test --test-isolation=none \"dist/test/**/*.test.js\"",
    "lint": "echo No lint configured for @kanban/adapters"
  },
  "dependencies": {
    "@kanban/utils": "0.1.0",
    "@kanban/contracts": "0.1.0",
    "@kanban/core": "0.1.0",
    "pg": "^8.13.1",
    "@supabase/supabase-js": "^2.49.2"
  },
  "devDependencies": {
    "@types/pg": "^8.11.11"
  }
}
